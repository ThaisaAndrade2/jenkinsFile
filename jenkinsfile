pipeline {
    agent any

    environment {
        APP_TOKEN = 'meu_token_pessoal'
    }

    stages {
        stage ('Start') {
            steps {
                echo 'Starting the pipeline'
            }
        }

        stage ('Print the token'){
            steps {
                echo APP_TOKEN
            }
        }
    }
}